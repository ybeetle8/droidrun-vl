# LangGraph 和 LangChain 依赖
# 使用最新版本（2025年）

# 核心框架
langgraph>=0.2.0          # LangGraph 核心
langchain>=0.3.0          # LangChain 核心
langchain-core>=0.3.0     # LangChain 核心组件
langchain-openai>=0.2.0   # OpenAI 集成（支持 OpenAI-compatible APIs）

# 可选：其他 LLM 提供商
# langchain-anthropic>=0.2.0
# langchain-google-genai>=2.0.0

# 类型检查
typing-extensions>=4.12.0

# 现有的 DroidRun 依赖保持不变
